import {Message} from "discord.js";

export default {
    handler: async (msg: Message) => {
        // This is the command executor
        // This needs to be registered in the '/lib/commands.ts'
    }
}
